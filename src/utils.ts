/**
 * Get a standardized title for the HTML `<title />` tag.
 * @param title Title to use.
 * @returns Standardized page title.
 */
export function pageHtmlTitle(title: string) {
    return `${title} ✧ COSMOS`;
}
